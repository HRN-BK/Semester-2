// Re-export the newly refactored form components from the `form` folder.
// This file exists for backward-compatibility so that existing imports such as
// `import { Form } from "@/components/ui/form"` continue to work.

export * from "./form/index"
